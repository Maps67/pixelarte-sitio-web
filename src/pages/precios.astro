---
import Layout from '../layouts/Layout.astro';

const plans = [
    {
        title: 'Tarjeta Digital',
        subtitle: 'Ideal para freelancers y profesionales.',
        price: '$4,990',
        priceSuffix: 'MXN + IVA',
        description: 'Una primera impresión impactante que abre puertas y genera conexiones.',
        features: [
            'Diseño 100% Personalizado',
            'Galería Multimedia (Fotos/Videos)',
            'Formulario de Contacto Directo',
            'Enlaces a Redes, Proyectos, etc.'
        ],
        highlighted: false,
        ctaText: 'Crear mi Tarjeta'
    },
    {
        title: 'Sitio de Crecimiento',
        subtitle: 'Para negocios que buscan generar clientes.',
        price: '$7,990',
        priceSuffix: 'MXN + IVA',
        description: 'La herramienta perfecta para construir tu marca y capturar leads de forma consistente.',
        features: [
            'Todo lo de la Tarjeta Digital, y:',
            'Página Web Landing Page Profesional',
            'Optimización para SEO inicial',
            'Gestión de 1 Red Social*'
        ],
        highlighted: true,
        ctaText: 'Lanzar mi Sitio'
    },
    {
        title: 'Solución a Medida',
        subtitle: 'Para empresas con necesidades únicas.',
        price: 'Desde $12,990',
        priceSuffix: 'MXN + IVA',
        description: 'El precio final se ajusta a la medida de la complejidad y las integraciones requeridas.',
        features: [
            'Todo lo del Sitio de Crecimiento, y:',
            'Sitio Web Completo (multi-página)',
            'Integración con Calendarios y Analíticas',
            'Blog o Sistema de Contenidos',
            'Soporte Prioritario'
        ],
        highlighted: false,
        ctaText: 'Diseñar mi Solución'
    }
];
---

<Layout title="Planes y Precios - Pixelarte Studio">
    <div class="container">
        <section class="page-header">
            <h1>Soluciones para Cada Etapa de tu Negocio</h1>
            <p>Elige el plan que te llevará de estar presente a ser relevante en el mundo digital.</p>
        </section>

        <section class="pricing-grid">
            {plans.map(plan => (
                <div class:list={["plan-card", { highlighted: plan.highlighted }]}>
                    {plan.highlighted && <div class="highlight-banner">MÁS POPULAR</div>}
                    <div class="plan-header">
                        <h3 class="plan-title">{plan.title}</h3>
                        <p class="plan-subtitle">{plan.subtitle}</p>
                        <p class="plan-price">
                            {plan.price}
                            {plan.priceSuffix && <span class="plan-price-suffix"> {plan.priceSuffix}</span>}
                        </p>
                        <p class="setup-fee">Setup inicial. Pago único.</p>
                        <p class="plan-description">{plan.description}</p>
                    </div>
                    <ul class="plan-features">
                        {plan.features.map(feature => (
                            <li>{feature}</li>
                        ))}
                    </ul>
                    <a href="/contacto" class="cta-button">{plan.ctaText}</a>
                </div>
            ))}
        </section>
        <div class="footnote-container">
            <p class="footnote">* No incluye costos de pauta publicitaria.</p>
        </div>
    </div>
</Layout>

<style>
    .container {
        padding-bottom: 6rem;
    }
    .page-header {
        text-align: center;
        padding: 4rem 0;
        max-width: 800px;
        margin: 0 auto;
    }
    .page-header h1 {
        font-size: 3rem;
        margin-bottom: 1rem;
    }
    .page-header p {
        font-size: 1.25rem;
        color: var(--color-texto-secundario);
    }

    .pricing-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 2rem;
        align-items: stretch;
        position: relative;
    }

    .plan-card {
        background-color: #ffffff;
        border: 1px solid var(--color-borde-claro);
        border-radius: 0.75rem;
        padding: 2.5rem;
        display: flex;
        flex-direction: column;
        text-align: center;
        box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        position: relative;
        /* ✅ INICIO: CAMBIO 1 - Transición suave para el efecto hover */
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        /* FIN: CAMBIO 1 */
    }
    /* ✅ INICIO: CAMBIO 2 - Efecto hover para tarjetas no destacadas */
    .plan-card:not(.highlighted):hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 25px -5px rgba(0,0,0,0.1), 0 8px 10px -6px rgba(0,0,0,0.1);
    }
    /* FIN: CAMBIO 2 */

    .plan-header {
        border-bottom: 1px solid var(--color-borde-claro);
        padding-bottom: 1.5rem;
        margin-bottom: 1.5rem;
    }
    .plan-title {
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
    }
    .plan-subtitle {
        font-size: 1rem;
        font-weight: 500;
        color: var(--color-acento);
        margin-bottom: 1.5rem;
        min-height: 40px;
    }
    .plan-price {
        font-family: 'Inter', sans-serif;
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 0.25rem;
        color: var(--color-texto-oscuro);
    }
    .plan-price-suffix {
        font-size: 1rem;
        font-weight: 500;
        color: var(--color-texto-secundario);
    }
    .setup-fee {
        font-size: 0.875rem;
        color: var(--color-texto-secundario);
        margin-bottom: 1.5rem;
    }
    .plan-description {
        color: var(--color-texto-secundario);
        min-height: 80px;
    }
    .plan-features {
        list-style: none;
        padding: 0;
        margin: 0 0 2rem 0;
        text-align: left;
        flex-grow: 1;
    }
    .plan-features li {
        margin-bottom: 1rem;
        display: flex;
        align-items: flex-start;
        color: var(--color-texto-secundario);
    }
    .plan-features li::before {
        content: '✔';
        color: var(--color-acento);
        margin-right: 0.75rem;
        font-size: 1.25rem;
        /* ✅ INICIO: CAMBIO 3 - Alineación perfecta del ícono */
        line-height: 1.2; /* Alinea verticalmente con la primera línea de texto */
        margin-top: 0.1rem; /* Pequeño ajuste para centrado perfecto */
        flex-shrink: 0; /* Evita que el ícono se encoja */
        /* FIN: CAMBIO 3 */
    }
    .cta-button {
        display: block;
        width: 100%;
        padding: 1rem;
        border-radius: 0.5rem;
        text-decoration: none;
        font-weight: 600;
        text-align: center;
        transition: all 0.2s ease;
    }
    
    .highlight-banner {
        background-color: var(--color-acento);
        color: #fff;
        font-weight: 700;
        font-family: 'Inter', sans-serif;
        font-size: 0.875rem;
        padding: 0.5rem 1rem;
        border-radius: 9999px;
        position: absolute;
        top: -1.25rem;
        left: 50%;
        transform: translateX(-50%);
        z-index: 20;
    }
    .plan-card.highlighted {
        background-color: var(--color-fondo-oscuro);
        border-color: var(--color-acento);
        transform: scale(1.05);
    }
    .plan-card.highlighted .plan-title { color: #fff; }
    .plan-card.highlighted .plan-subtitle { color: var(--color-acento); }
    .plan-card.highlighted .plan-price,
    .plan-card.highlighted .plan-price-suffix,
    .plan-card.highlighted .setup-fee,
    .plan-card.highlighted .plan-description,
    .plan-card.highlighted .plan-features li { color: var(--color-texto-claro); }
    .plan-card.highlighted .plan-header { border-bottom-color: var(--color-borde); }
    .plan-card.highlighted .cta-button { background-color: var(--color-acento); color: #fff; }
    .plan-card.highlighted .cta-button:hover { opacity: 0.9; transform: scale(1.02); }
    
    .plan-card:not(.highlighted) .cta-button {
        background-color: transparent;
        border: 1px solid var(--color-acento);
        color: var(--color-acento);
    }
    .plan-card:not(.highlighted) .cta-button:hover {
        background-color: var(--color-acento);
        color: #fff;
    }

    .footnote-container {
        text-align: center;
        margin-top: 3rem;
    }
    .footnote {
        font-size: 0.875rem;
        color: var(--color-texto-secundario);
    }
</style>


