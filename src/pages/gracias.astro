---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Agenda tu Sesión - Pixelarte Studio">
    <div class="container">
        <div class="booking-wrapper">
            <div class="booking-header">
                <h1>¡Excelente! Solo falta un paso.</h1>
                <p>Hemos recibido tu solicitud para el diagnóstico. Para continuar, por favor, elige la fecha y hora que mejor te convenga para nuestra sesión de 30 minutos.</p>
            </div>

            {/* ===== INICIO: ESPACIO PARA CALENDLY ===== */}
            <div class="calendly-embed-container">
                
                <!-- Principio del widget integrado de Calendly -->
                <div class="calendly-inline-widget" data-url="https://calendly.com/contacto-pixelartestudio" style="min-width:320px;height:700px;"></div>
                <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
                <!-- Final del widget integrado de Calendly -->

            </div>
            {/* ===== FIN: ESPACIO PARA CALENDLY ===== */}

        </div>
    </div>
</Layout>

<style>
    .container {
        max-width: 1000px;
        margin: 0 auto;
        padding: 4rem 2rem;
    }
    .booking-wrapper {
        background-color: #ffffff;
        border-radius: 0.75rem;
        box-shadow: 0 10px 25px -5px rgba(0,0,0,0.1);
        overflow: hidden;
    }
    .booking-header {
        text-align: center;
        padding: 2.5rem 2rem;
        border-bottom: 1px solid var(--color-borde-claro);
    }
    .booking-header h1 {
        font-size: 2rem;
        margin-bottom: 1rem;
        color: var(--color-texto-oscuro);
    }
    .booking-header p {
        color: var(--color-texto-secundario);
        font-size: 1.125rem;
        max-width: 650px;
        margin: 0 auto;
    }

    .calendly-embed-container {
        /* -- Este estilo es recomendado por Calendly para que sea responsivo -- */
        position: relative;
        min-width: 320px;
        height: 700px;
        overflow: hidden;
    }

    /* -- Estilos para el iframe que Calendly insertará -- */
    .calendly-embed-container :global(iframe) {
        position: absolute;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
    }
</style>

