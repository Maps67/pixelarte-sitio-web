---
// --- INICIO DEL FRONTMATTER ---
// Es posible que aquí tengas código JavaScript/TypeScript.
// Por ejemplo, la importación de componentes o la definición de props.
// Deja tu código existente aquí como está.
export interface Props {
	title: string;
}

const { title } = Astro.props;
---
<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<title>{title}</title>

		<link rel="icon" type="image/png" href="/favicon.png" />

		</head>
	<body>
		<slot />
	</body>
</html>